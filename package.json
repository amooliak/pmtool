{
  "name": "pmtool",
  "version": "1.0.0",
  "private": true,
  "description": "Accessible project portfolio dashboard built with HTML, CSS, and JavaScript.",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  }
}
